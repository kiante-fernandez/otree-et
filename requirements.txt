# oTree 6.x. Install with: pip install -r requirements.txt
#
# Pinned major version: oTree's templating syntax has changed across major
# releases, and this project is verified against 6.x.
otree>=6.0,<7.0

# oTree runs uvicorn internally ('otree prodserver'); pin it so a deployment
# cannot resolve a version without websockets support.
uvicorn[standard]>=0.20

# Only needed for the end-to-end browser test (tests/smoke_e2e.py):
#   pip install playwright && python -m playwright install chromium
